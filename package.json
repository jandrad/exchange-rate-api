{
    "name": "exchange-rate-api",
    "version": "1.0.0",
    "license": "MIT",
    "private": true,
    "scripts": {
        "dev": "pnpm run build && wrangler dev",
        "build": "cpy index.html favicon.ico openapi.yml dist && wrangler pages functions build functions --outdir .worker --project-directory . --build-output-directory dist",
        "deploy": "wrangler deploy",
        "typegen": "wrangler types"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^5.20260708.1",
        "@types/node": "^20.14.5",
        "cpy-cli": "^5.0.0",
        "typescript": "^5.4.5",
        "wrangler": "^4.0.0",
        "@biomejs/biome": "^1.3.1"
    }
}
